---
import Layout from "@/components/layout/Layout.astro";

import { feeds } from "@/lib/news/feeds/feedManager";

import { faSquareUpRight } from "@fortawesome/free-solid-svg-icons";
import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
---

<Layout title="Subscribe with your Feed Reader">
	<div class="mx-auto flex w-full max-w-4xl flex-col gap-6 p-8">
		<p class="text-sm">
			The original/upstream news sources we use are published in a variety of
			formats. For your convenience, Scouting411 parses and re-exports these
			sources as a set of standards-compliant, non-paginated web feeds for you.
			If you don't know what a web feed is, you can learn more at{" "}
			<a
				href="https://aboutfeeds.com/"
				rel="noopener noreferrer"
				target="_blank"
				class="text-primary wrap-anywhere hyphens-auto hover:underline"
				>aboutfeeds.com</a
			>.
		</p>

		<div class="rounded-lg border p-4">
			<div class="text-muted-foreground mb-2 text-xs font-medium uppercase">
				Caveats
			</div>
			<ul class="flex flex-col gap-1.5 text-sm">
				<li class="flex gap-2">
					<span class="text-muted-foreground">&bull;</span>
					The feeds are not continuously updated. They may be stale by up to 24 hours
					at a given time.
				</li>
				<li class="flex gap-2">
					<span class="text-muted-foreground">&bull;</span>
					We have not added support for media enclosures; these feeds will not work
					with a podcast app.
				</li>
				<li class="flex gap-2">
					<span class="text-muted-foreground">&bull;</span>
					While we have attempted to faithfully represent the original content, the
					metadata and formatting of these feeds is not identical to the upstream
					sources.
				</li>
			</ul>
		</div>

		<div
			class="border-primary/30 bg-primary/5 flex flex-col items-start gap-2 rounded-lg border p-4 px-5 sm:flex-row sm:items-center sm:justify-between"
		>
			<div>
				<div class="mb-2 font-serif font-bold">Import All Feeds</div>
				<p class="text-muted-foreground text-sm">
					Add every Scouting411 feed to your reader at once via OPML.
				</p>
			</div>
			<a
				href="/feeds/all/opml"
				rel="noopener noreferrer"
				target="_blank"
				class="bg-primary hover:bg-primary/90 text-primary-foreground flex items-center gap-2 rounded-md px-4 py-2 text-sm font-semibold whitespace-nowrap"
			>
				Open OPML
				<FontAwesomeIcon icon={faSquareUpRight} height="0.9em" />
			</a>
		</div>

		<div class="overflow-hidden rounded-lg border">
			<table class="w-full border-collapse text-sm">
				<thead>
					<tr class="border-b">
						<th class="px-4 py-3 text-left font-medium">Source</th>
						<th class="px-4 py-3 text-left font-medium">Feeds</th>
					</tr>
				</thead>
				<tbody>
					{
						feeds.map((feed) => (
							<tr class="border-gray-2 hover:bg-muted border-b last:border-0">
								<td class="px-4 py-3">
									<a
										class="text-primary font-medium wrap-anywhere hyphens-auto hover:underline"
										href={feed.urls.overview}
									>
										{feed.name}
									</a>
								</td>
								<td class="px-4 py-3">
									<div class="flex gap-2">
										<a
											href={feed.urls.rss}
											rel="noopener noreferrer"
											target="_blank"
											class="hover:border-primary hover:text-primary rounded-md border px-2.5 py-1 text-xs font-medium transition-colors"
										>
											RSS
										</a>
										<a
											href={feed.urls.atom}
											rel="noopener noreferrer"
											target="_blank"
											class="hover:border-primary hover:text-primary rounded-md border px-2.5 py-1 text-xs font-medium transition-colors"
										>
											Atom
										</a>
									</div>
								</td>
							</tr>
						))
					}
				</tbody>
			</table>
		</div>
	</div>
</Layout>
